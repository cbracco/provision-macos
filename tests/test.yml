- hosts: all
  connection: local
  roles:
    - role: homebrew
      vars:
        # Only install packages necessary for testing
        homebrew_packages:
          - python
          - python@2
          - python3
          - ruby
        homebrew_cask_packages:
          - font-roboto-mono
          - visual-studio-code
    - dotfiles
    - macos
    - pip
    - ruby
    - vscode
