- hosts: all
  connection: local
  roles:
    - computername
    - role: homebrew
      vars:
        # Override default packages here for quicker test builds
        homebrew_packages:
          - pv
          - ssh-copy-id
          - wget
        homebrew_cask_packages:
          - firefox
          - font-roboto-mono
